#include "TimeManager.h"
#include <stdint.h>
#include "Linklist.h"

void timeManage(uint32_t baseTime, uint32_t currentTime, uint32_t rate, struct Linkedlist *newList){
  	 // if(newList->Head == NULL)
	 // {
     // addList(createLinkedElement(3), createLinkedList());
      // newList->Head = newElement;
      // return newList->Head->actionTime; 
	 // }
  // uint32_t interval = currentTime - baseTime;
  
}